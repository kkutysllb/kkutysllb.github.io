本地预览：`npm run dev` 或 `python3 -m http.server 4173` 后访问 `http://localhost:4173`；Vercel 部署：`vercel dev` 或直接连接 GitHub 仓库选择 Other Framework 一键部署。新增产品只需在 `data/projects.yml` 追加条目并在 `repos.txt` 加一行映射，无需改动代码。